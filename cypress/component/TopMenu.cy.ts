// @ts-ignore
describe('TopMenu.cy.ts', () => {
  it('Top menu component test', () => {
    //cy.mount(<TopMenu />)

    // Checking the value of contact information
   //cy.get('#supportEmail').should('contain.text', Cypress.env('support_email'))
  })
})
